from setuptools import setup, find_packages

setup(
    name="yugenai",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # Add your project's dependencies here
        # They can be listed in requirements.txt instead
    ],
    # Add any additional configuration here
)
